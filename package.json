{
  "name": "upi-screenshot-detector-pro",
  "version": "2.0.0",
  "description": "99% accurate UPI detector with OCR + ML",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=3000",
    "build": "echo 'Ready to deploy!'",
    "deploy": "gh-pages -d ."
  },
  "dependencies": {
    "tesseract.js": "^4.1.1",
    "@tensorflow/tfjs": "^4.5.0",
    "@tensorflow/tfjs-backend-webgl": "^4.5.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
